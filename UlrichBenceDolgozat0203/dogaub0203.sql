CREATE TABLE users(
                    ID int PRIMARY KEY AUTO_INCREMENT,
                    userName varchar(255),
                    email  varchar(255),
                    passwordHash varchar(255)
)